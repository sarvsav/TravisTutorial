language: cpp

branches:
  only:
   - master
before_install:
  - sudo apt-get install intltool libcap-dev libjson0 libjson0-dev libjson-c-dev
  - wget https://s3.amazonaws.com/json-c_releases/releases/json-c-0.12.tar.gz
  - tar -xzvf json-c-0.12.tar.gz
  - sed -i s/-Werror// Makefile.in && ./configure --prefix=/usr --disable-static && make -j1
  - git clone git://anongit.freedesktop.org/pulseaudio/pulseaudio
  - cd pulseaudio
  - ./bootstrap.sh
  - make
  - sudo make install
  - sudo ldconfig
  - pulseaudio --version
before_script: ls

script:
  - make -C Build
